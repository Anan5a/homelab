version: "3.8"
networks:
  homelab-net:
    name: homelab-network # Explicitly name the network to avoid project name prefixing
    driver: bridge
  internal-net:
    name: internal-network # Explicitly name the network to avoid project name prefixing
    driver: bridge
